<?xml version="1.0" encoding="utf-8"?>
<!--
    Built with RemoteViews rather than Glance. A widget is read by glancing at
    it, so what it needs is to be correct and to cost nothing; the framework
    that draws it is invisible. This also keeps the widget clear of the Compose
    and R8 version churn that has broken the release build twice.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="vertical"
    android:padding="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:baselineAligned="false"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/widget_headline"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:maxLines="1"
            android:textColor="@color/naz_white"
            android:textSize="15sp"
            android:textStyle="bold" />

        <!--
            Capture, from the home screen. The one thing worth doing here that
            reading cannot: a thought arrives away from the desk and the vault
            is the place it belongs.
        -->
        <TextView
            android:id="@+id/widget_capture"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/capture_title"
            android:paddingStart="10dp"
            android:paddingEnd="2dp"
            android:text="@string/widget_capture_glyph"
            android:textColor="@color/naz_white"
            android:textSize="18sp" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/widget_tasks"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:orientation="vertical" />
</LinearLayout>
