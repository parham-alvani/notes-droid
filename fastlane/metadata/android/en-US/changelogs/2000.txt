A link to a heading now lands on the heading. 48 links in the vault name one, and every one of them opened the note at the top instead — the heading was read from the link and then thrown away. The preview says which heading it will land on, and a link to a heading in the same note scrolls there instead of reporting itself broken.

A heading that has since been renamed is not an error: the note opens where you left it.
